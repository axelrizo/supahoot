import { testId } from '@/test/support/test-utils'

export const CREATE_LOBBY_BUTTON = testId('create-lobby-button')
export const LOBBY_NAME_INPUT = testId('lobby-name-input')
export const LOBBY_FORM = testId('lobby-form')
export const LOBBY_TITLE = testId('lobby-name')
